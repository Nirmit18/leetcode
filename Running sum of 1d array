class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        int c=0,i,r;
        vector<int>v;
        for(i=0;i<nums.size();i++){
            c=c+nums[i];
            v.push_back(c);
        }
return v;
    }
};
